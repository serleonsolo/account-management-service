# README for account-management-service

Run service
```bash
account-service/account-service-application$ mvn spring-boot:run -Dspring.profiles.active=development
```

Start docker
```bash
/account-service/dev>docker-compose up -d --build
```
